# Commands and Permissions

This page is the new home for command and permission reference.

## Current status

Command and permission tables are being migrated and verified from legacy wiki content.

## Admin notes

- Validate permission nodes in a staging environment before deploying to production.
- Group permissions by role to avoid accidental admin access.
- Document local policy decisions (for example, which commands moderators can run).

## In progress

The next update will include:

- Core player command list.
- Admin command list.
- Permission node matrix with defaults.
