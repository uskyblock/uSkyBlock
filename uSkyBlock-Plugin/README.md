# uSkyBlock-Plugin

This module is the main plugin module. Its responsibility is to gather the artifacts
from the other modules and produce a `uSkyBlock.jar` (via the Shadow plugin) suitable for deploy on a Bukkit server.
